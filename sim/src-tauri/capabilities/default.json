{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Window control the game needs: fullscreen toggle, minimise, dragging, and closing. `destroy` is what actually closes a window in Tauri v2 -- a close request that the frontend prevents (so a run in progress can be written out first) has to be finished off explicitly.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-start-dragging"
  ]
}
